meta {
  name: Crear Usuario
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users
  body: json
  auth: none
}

body:json {
  {
    "username": "victor_hugo",
    "email": "victor@uneg.edu.ve",
    "password": "Password123!",
    "name": "Victor Hugo"
  }
}

tests {
  test("Debe crear el usuario (201)", function() {
    expect(res.getStatus()).to.be.oneOf([201, 409]);
  });

  test("No debe devolver la contraseña", function() {
    const data = res.getBody();
    expect(data.password).to.be.undefined;
  });
}
